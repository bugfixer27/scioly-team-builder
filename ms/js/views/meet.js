// The middle school form has no meeting-time questions, so the "when can they meet" widgets are inert here.
// They keep the high school builder's signatures so the shared view code needs no special cases.
export function timesChip() { return ''; }
export function meetBadge() { return ''; }
export function matchWith() { return null; }
